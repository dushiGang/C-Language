/*
-------------------------------------------Variables------------------------------------------------------------

Variables are the names of memory locations where we store Data.

1.When we run any program, it consumes memory in RAM till its execution in process. To perform a 
task program can use this memory for various reasons. One of the important reasons is to store 
needful data, which is going to be used in processing. In addition of two numbers program, program 
needs to preserve two data until addition task is over. We need to reserve memory blocks to store 
these data, and these memory blocks should be named for future reference in the program. 

2.It depends on the program, how many memory blocks are required to handle data. In addition of 
two numbers, three memory spaces should be reserved, two blocks for two numbers that to be 
added and third block is to store result. 
These memory blocks are variables. They have names for identification.

Variable naming Rules:

1.Variable name is any combination of alphabet, digit and underscore. No other symbol is permitted. 
 
2.Valid variable name cannot start with digit.      (Matlab phla letter digit nhi ho sakta doosra teesra leeter hum digit rakh sakte hain)


valid                       Invalid
a                           a$
a25                         A.b
a_2_b_3                     x-1





















*/
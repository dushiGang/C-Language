/*
----------------------------------------------Keywords-----------------------------------------------------------

1.Keywords are predefined words; their meaning is already known to compiler. Every word we are using in the 
program must have known to compiler. If the meaning is not known to compiler then it will yield an error. 
To avoid error, we have to declare such words to compiler by declaration statements.

2.Keywords are the words, whose meaning is already defined; we can directly use them for specific reason. 

3.There are 32 keywords according to the ANSI standard, but sometimes compiler may support 
additional keywords too.
auto,break,case,char,continue,const,do,double,default,enum,else,extern,for,float,goto,if,int,long,register,return
short,signed,sizeof,static,struct,switch,typedef,union,unsigned,void,volatile,while.


Note=Keywords ko hum predefined Words or reserved Words bhi bolte hain











*/